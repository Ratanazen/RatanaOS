#include <tunables/global>

/usr/bin/ratana-system-manager {
  #include <abstractions/base>

  /usr/bin/ratana-system-manager mr,
  /var/log/ratana-system.log rw,
  /etc/ratanaos/** r,

  # Deny all other access
  deny network,
}
