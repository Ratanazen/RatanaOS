# RatanaOS TODO

- Install Qt6, GTK4, `pkg-config`, and CMake in the build environment
- Connect desktop shell quick actions to runnable system services
- Add backend integration for updates, software discovery, and telemetry
- Add end-to-end application and compositor tests on a Wayland session
