# Architecture

RatanaOS uses Debian `live-build`.
* `config/` contains the live-build configuration.
* `apps/` contains custom applications compiled during the chroot phase.
* `branding/` and `themes/` contain the custom visuals.
