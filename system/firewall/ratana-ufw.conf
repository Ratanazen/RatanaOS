# RatanaOS Default UFW Configuration
# Managed by Ratana System Installer

# Set default policies
DEFAULT_INPUT_POLICY="DROP"
DEFAULT_OUTPUT_POLICY="ACCEPT"
DEFAULT_FORWARD_POLICY="DROP"
DEFAULT_APPLICATION_POLICY="SKIP"

# Enable logging
LOGLEVEL="low"

# Deny all incoming by default, but allow standard web surfing (outgoing handled by default)
# Custom rules can be appended here by the user.
