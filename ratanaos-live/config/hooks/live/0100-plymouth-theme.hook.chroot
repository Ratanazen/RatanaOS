#!/bin/sh
set -e
if [ -d /usr/share/plymouth/themes/ratanaos ]; then
    if command -v plymouth-set-default-theme >/dev/null 2>&1; then
        plymouth-set-default-theme -R ratanaos || true
    fi
fi
