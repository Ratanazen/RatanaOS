# ISO Validation Report

## Hybrid Validation
- **BIOS Boot**: Enabled via `i386-pc` El Torito image.
- **UEFI Boot**: Enabled via `x86_64-efi` image.
- **ISO Standard**: ISO 9660 Joliet extension compliant.

## Checksums
The SHA256 checksum was correctly generated and written to `output/SHA256SUMS`.
The rebuilt ISO is structurally sound and ready for VM and hardware deployment.
